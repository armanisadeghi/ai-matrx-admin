import GenerateShellClient from "./GenerateShellClient";

/**
 * /image-studio/generate
 *
 * Header + outer chrome are owned by `(tools)/layout.tsx`.
 */
export default function GeneratePage() {
  return <GenerateShellClient />;
}
