// app/(ssr)/ssr/notes/[noteId]/page.tsx — Route stub
// Returns null — the NotesWorkspace in the layout handles all rendering.
// This file exists purely for Next.js route matching at /ssr/notes/[noteId].

export default function NotePage() {
  return null;
}
