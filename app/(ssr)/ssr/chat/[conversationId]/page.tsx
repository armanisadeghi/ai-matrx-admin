// app/(ssr)/ssr/chat/[conversationId]/page.tsx
// Null page stub for conversation routes.
// The ChatWorkspace reads the conversation ID from the URL and handles loading.

export default function ConversationPage() {
    return null;
}
