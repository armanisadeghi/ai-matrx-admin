export const metadata = {
    title: 'Content Manager — AI Matrx',
    description: 'Manage your websites, pages, and content',
};

export default function ContentLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
