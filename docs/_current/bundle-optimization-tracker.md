# Bundle / Build-Time Optimization Tracker

Live working doc. Update the **Status** and **Notes** columns as we go. Add new rows when we find new candidates.

---

## Corrections — things I claimed that were wrong

| # | What I said | The truth |
|---|---|---|
| 1 | `onnxruntime-web` (133 MB on disk) is unused / a removal candidate | **Wrong.** It's a runtime dep of `@ricky0123/vad-react` (used by `voice/voice-assistant-ui/Assistant.tsx` and the `useVoiceChat*` hooks). The 133 MB on disk is mostly WASM, and the codebase points VAD's `onnxWASMBasePath` at the **jsdelivr CDN**, so the WASM doesn't ship in our bundle. The JS surface that *does* ship is small. Disk size ≠ bundle size — I conflated them. **Skip.** |
| 2 | Delete the 4 files in `features/code-editor/components/unused/` to reduce bundle size | **Misleading.** If a module has zero importers, Turbopack/webpack already strips it out of every bundle — they contribute **0 bytes** to runtime. The real (small) cost is build-time *parsing*: every file the analyzer touches, even if shaken later, takes time to lex, parse, resolve imports, walk the graph. So deleting them is a minor build-time win, not a bundle win. I'll only call this out when build-time matters; it does **not** belong on the bundle list. |
| 3 | Lobehub will require a hostile uninstall | You handled it cleanly by **inlining the 11 provider icons** into `model-provider-icons.tsx`. `pnpm why` now shows: `lucide-react 0.577.0` (single version), no `antd`, no `@lobehub/*`. ✅ The whole subtree is gone. |

---

## Ground rules going forward

1. **Disk size ≠ bundle size.** Many heavy packages ship native binaries, WASM, or assets that are loaded at runtime from CDN, not bundled. I'll only flag a package when I can show it's actually pulled into a client chunk via static import.
2. **No "delete the unused folder" suggestions** unless they're imported somewhere or there's a measured build-time impact.
3. **Always verify with `pnpm why`** before claiming something is or isn't in the dep tree.
4. **No more "rip it out" by default.** Inlining, replacing with a smaller equivalent, or just lazy-loading is usually better.

---

## Status board

Legend:  
`▢` not started  `→` in progress  `✓` done  `✗` decided no-go  `?` needs investigation

### Tier 1 — confirmed wins

| # | Item | What's actually true | My recommendation | Your notes | Status |
|---|---|---|---|---|---|
| 1 | `@lobehub/icons` removal | One file (`ModelSelection.tsx`) was importing 11 brand icons; that pulled in `antd`, `antd-style`, `@ant-design/icons{,-svg}`, plus 2 duplicate `lucide-react` major versions | Inline the 11 SVGs and drop the dep | Done — extracted icons into `features/chat/components/input/model-provider-icons.tsx` and removed the package | ✓ |
| 2 | ~~`monaco-editor` value→type imports~~ | **False alarm — already done.** The exact regex `from\s+['"]monaco-editor['"]` finds only 4 files, and all 4 are already `import type`. My earlier "13 files" count was actually `@monaco-editor/react` (the small React wrapper, not the same package). Also: in `monaco-config.ts` Monaco is loaded from CDN (`cdn.jsdelivr.net`) via `loader.config()` — Monaco's runtime is **never bundled**. Bundle impact already zero. | No-op. Moved to Tier 3. | | ✗ |
| 3 | `pdfjs-dist` deduplication | No file in the repo imports `from "pdfjs-dist"` directly (verified). The two PDF previewers import `pdfjs` as a named export from `react-pdf`. Top-level `pdfjs-dist@5.5.207` was completely unused; `react-pdf@10.4.1` ships its own `pdfjs-dist@5.4.296` (locked, not a peer range). | Remove `"pdfjs-dist"` line from `package.json` `dependencies`. After `pnpm install`, only `5.4.296` remains active. No code changes needed. | Done — `pnpm why pdfjs-dist` now shows a single `5.4.296` via `react-pdf`. Verified working on PDF previewer routes. | ✓ |
| 10 | Layout-reachable type imports must be `import type` | `types/reduxTypes.ts` is imported by **every** layout (`(authenticated)/layout.tsx`, `(a)/layout.tsx`, `(public)/PublicProviders.tsx`, `(legacy)/layout.tsx`, `(ssr)/layout.tsx`, `Providers.tsx`, `EntityProviders.tsx`) **and** every redux store/slice/middleware file (~21 importers total). Inside `reduxTypes.ts` the imports `Database` (from `database.types.ts`, **24,174 LOC**), `UserData` (from `userDataMapper`), and `GlobalCacheState` (from `globalCacheSlice.ts`, which itself reaches `entityTypes` → `initialSchemas.ts` **115,992 LOC**) were all **plain named imports without the `type` keyword**. Under `isolatedModules: true`, SWC must keep those module references unless the syntactic `type` keyword tells it otherwise. Net result: every layout was forcing Turbopack to walk the full Supabase generated types and the entity schema chain just to satisfy a few type aliases. Same pattern existed in `emptyGlobalCache.ts` (statically imported by `(authenticated)`, `(a)`, `(ssr)` layouts at module scope), `app/(authenticated)/layout.tsx` itself (`InitialReduxState`), `(a)`/`(ssr)` layouts (`InitialReduxState` + `UserData`), `features/ai-models/types.ts` (`Database`), `lib/redux/store.ts` (`UserPreferences`/`UserPreferencesState` mixed in with a real value import), and 9 redux slice/dynamic/saga files (`FeatureName`, `SliceState`, `DeleteResponse`, `PaginatedResponse`, all the thunk arg types). Every one of those was syntactically a non-type import of a pure-type symbol. | Convert all of them to `import type`. Where a file mixes value + type imports from the same module, split into two `import` statements (value + `import type`). Inside `reduxTypes.ts`, also added a doc comment at the top documenting the contract: every named import in this file MUST be type-only because of how widely it's reused. Inside `emptyGlobalCache.ts`, same contract documented inline — `getEmptyGlobalCache` only needs the entity types as types, never as values, so the slim path's static graph is now fully decoupled from `entityTypes` / `initialSchemas` / `fullRelationships`. **Files touched:** `types/reduxTypes.ts`, `utils/schema/schema-processing/emptyGlobalCache.ts`, `app/(authenticated)/layout.tsx`, `app/(a)/layout.tsx`, `app/(ssr)/layout.tsx`, `features/ai-models/types.ts`, `lib/redux/store.ts`, `lib/redux/slices/featureSliceCreator.ts`, `lib/redux/dynamic/normalizers.ts`, `lib/redux/dynamic/featureSchema.ts`, `lib/redux/selectors/featureSelectors.ts`, `lib/redux/actions/featureActions.ts`, `lib/redux/concepts/buEntitySlice.ts`, `lib/redux/middleware/sagas.ts`, `features/registered-function/redux/registeredFunctionReducer.ts`, `features/registered-function/hooks/useRegisteredFunction.ts`. ReadLints clean. | Push and watch the next Vercel build. The slim path's static graph just lost a transitive reference to `database.types.ts` (24k LOC) and `globalCacheSlice` (which dragged in `@reduxjs/toolkit`'s `createSlice` machinery + the entity type chain). Expected biggest gain on `(authenticated)`/`(a)`/`(ssr)`/`(public)` route entries — the (legacy) layout still legitimately preloads the entity schema by design. | ✓ |
| 11 | `optimizePackageImports` audit — confirmed nothing to add | Per [Next.js docs](https://nextjs.org/docs/app/api-reference/config/next-config-js/optimizePackageImports), the default auto-optimized list already includes `lucide-react`, `date-fns`, `lodash-es`, `recharts`, `framer-motion`, `@tabler/icons-react`, `react-icons/*`, and `@radix-ui/react-icons`. Explicit `optimizePackageImports` entries merge with the defaults — explicit listing of an already-defaulted package is a no-op for build time. The `motion` (new package) replacement isn't used in the codebase (0 imports). `@xyflow/react` is only imported from feature files under `features/workflows*` and `lib/redux/workflow*` — never from the shell, so it's only loaded on workflow routes. | No changes to `next.config.js`. The current `['lucide-react', 'zustand']` list is fine — `zustand` isn't on the auto list, `lucide-react` is redundant but harmless. | | ✓ |
| 9 | Global shell static-import audit (`Providers.tsx`, `DeferredSingletons.tsx`, `PublicProviders.tsx`) | These 3 files are in the static dep graph of *every* authenticated/public route. The legacy lobehub cut (22→9 min) proved the shell is the highest-leverage place. **First pass (incorrect):** wrapped leaf widgets in `next/dynamic({ ssr: false })` from inside the wrappers themselves. This was wrong on two counts: (1) `app/Providers.tsx` is a Server Component and `next/dynamic({ ssr: false })` is invalid there; (2) even when valid (PublicProviders, DeferredSingletons), it pushes the lazy-loading concern UP into the wrapper instead of DOWN into each leaf — every consumer of the leaf widget would have to repeat the dance. **Corrected pattern (the "thin shell + Impl" pattern):** the wrapper stays a static-import shell. Each heavy leaf widget is rewritten as a 2-file pair — `<Name>.tsx` is a tiny `"use client"` shell that statically imports nothing heavy, optionally gates on a cheap selector or local listener, and renders `<Name>Impl />` via `next/dynamic({ ssr: false, loading: () => null })`; `<Name>Impl.tsx` holds all the heavy imports + JSX. Refactored 7 leaf widgets this way: `RecoveryWindow` (selector-gated on `isOpen`), `GlobalTaskShortcut` (keydown listener stays in shell, modal lazy on first ⌘⇧T), `AudioRecoveryToast` (gated on `hasRecoveredData`), `AuthSessionWatcher` (supabase listener in shell, modal lazy on `SIGNED_OUT`), `AnnouncementProvider` (gated on `shellDataLoaded`), `UnifiedOverlayController` (the 860-LOC windowRegistry now lives behind dynamic), `LegacyPromptOverlaysController` (24+ promptRunnerSlice selectors now behind dynamic), and `CloudFilesPickerHost` (split into `cloudFilesPickerOpeners.ts` for the imperative API + `CloudFilesPickerHostImpl.tsx` for the dialog body). `RecoveryNudge`, `CreateTaskFromSourceDialog`, and `PersistentDOMConnector` were skipped — each is <90 LOC with no unique heavy imports, so refactoring them adds files for zero benefit. The other shell-level fixes from the first pass remain in place: barrel-of-barrels `@/lib/redux/brokerSlice` replaced with direct `@/lib/redux/brokerSlice/slice` import + `await import()` inside the idle callback; `fetchFullContext` lazy-imported inside its idle callback; `InitialReduxState` is `import type`. | **Rule going forward:** anything in the static dep graph of `app/Providers.tsx`, `app/DeferredSingletons.tsx`, or `(public)/PublicProviders.tsx` MUST be either (a) trivial (no heavy unique deps), (b) `import type`-only, or (c) a thin shell that internally `next/dynamic`s its `<Name>Impl` sibling. The build-time contract is documented inline at the top of `DeferredSingletons.tsx` and reiterated by example in each refactored leaf widget. **Never** add `dynamic()` from inside a Server Component (`Providers.tsx` is one) and **never** wrap leaf widgets in `dynamic()` from a wrapper file — that's the wrong layer. | Awaiting build-time measurement after the corrected refactor. | → |

### Tier 2 — needs investigation, no claim yet

| # | Item | What I actually know | What I'd need to verify before recommending anything | Your notes | Status |
|---|---|---|---|---|---|
| 4 | `react-pdf` pulled into a non-PDF route | `BinaryFilePdfPreview.tsx` and `PdfPreview.tsx` both `import { Document, Page, pdfjs } from "react-pdf"` directly. Their authoring comments say "lazy-loaded by parent." | Confirm parents (`BinaryFileViewer`, `FilePreview.tsx`) use `dynamic(() => import('./PdfPreview'), { ssr: false })`. If yes → already optimal, skip. If no → wrap. | | ? |
| 5 | `@tabler/icons-react` (50+ files) and `react-icons/*` (70+ files) | Both support per-icon tree-shaking when imported by sub-path. | Check `next.config.ts` for `experimental.optimizePackageImports`. If `@tabler/icons-react` is missing, adding it could shrink chunks meaningfully. | | ? |
| 6 | `framer-motion` | Heavy library, used in many files. Already tree-shakes per-export reasonably well. | Worth checking the analyzer's biggest chunks for the `motion` cost before assuming a problem. | | ? |
| 7 | `react-syntax-highlighter` | 8 files importing it. Default import pulls every language grammar (huge). | Check whether files use the deep import (`react-syntax-highlighter/dist/cjs/light` + register-only-needed-languages) vs. the default heavy entry. If they use the heavy entry, switching to `light` build is a known big win. | | ? |
| 8 | `@xyflow/react` (2.7 MB) | 80+ files import it. It's the workflow canvas — that whole feature genuinely needs it. | Verify the workflow routes are the only ones pulling it (no accidental import from a globally-loaded shell). | | ? |

### Tier 3 — explicitly *not* on the list

| Item | Why I'm not recommending action |
|---|---|
| `monaco-editor` runtime | Loaded from CDN via `loader.config({ paths: { vs: 'cdn.jsdelivr.net/...' } })` in `monaco-config.ts`. Not bundled. Disk size is misleading. The 4 files importing `monaco-editor` directly are all `import type` (erased at compile). The wrapper `@monaco-editor/react` is small (~20 KB) and necessary. |
| `onnxruntime-web` | Used by voice assistant. WASM is CDN-loaded, JS surface is small. Disk size is misleading. |
| `dashjs`, `hls.js` | Found 0 static imports in app code. Likely transitive from a video lib. Need a `pnpm why` before any recommendation — but probably fine. |
| `three` | Only `components/ui/canvas-reveal-effect-impl.tsx` uses it. That file is already lazy-loaded by the wrapper pattern. Skip. |
| `mermaid` | Already lazy via the markdown diagram block. Skip. |
| `posthog-js` | Loaded through analytics provider; not a static client-bundle import. Skip. |
| `@babel/standalone` | Already lazy-loaded as of last round. ✓ |
| Files in `code-editor/components/unused/` | If unused, they don't ship. Deleting them is a build-time micro-optimization at best, not a bundle win. |

---

## Open questions for you

- **#9 shell audit** — corrected pattern is now in place. Push and watch the next Vercel build. The expected win is build *time*, not chunk size — every page entry's static graph just shed 7 heavy subtrees (notably the 860-LOC `windowRegistry` and the `promptRunnerSlice` selector set), each replaced by a 4-line `dynamic()` shell. Compare against the 7m21s baseline measured after the first pass.
- **Tier 2 items** — once the next analyzer report is in, pick the heaviest remaining offender. Likely candidates: any layout/page that *itself* statically imports something heavy, or `react-syntax-highlighter` if it's using the default heavy entry.

---

## Analyzer report — 2026-04-28 (post-run, data-driven)

Ran `pnpm build:analyze:save` locally (7.8 min on Mac Studio). Wrote a parallel parser at `scripts/analyze-routes.py` that:
1. Walks all 948 route `analyze.data` files
2. Reconstructs full source paths via `parent_source_index` chain
3. Aggregates module-route reach across the whole app

### What the data actually shows

**The build-time problem is not a few "fat" routes — it is a fat *baseline* graph that every route inherits.**

| Stat | Value |
|---|---|
| Total routes analyzed | **948** |
| Routes ≥10 MB analyze.data (heavy chunk graph) | **598 (63%)** |
| Routes ≥12 MB | 98 |
| Median modules per route (project files only) | **8,674** |
| P90 modules per route | 8,792 |
| Max modules per route | 9,702 (admin/official-components/[componentId]) |
| Min real-page modules | ~241 (api routes) |

The variance between the median page (8,674 modules) and the worst page (9,702) is only **+12%**. That tells us the BASELINE shell+layout graph is ~8,500 modules per page. Reducing the baseline by 1 module saves ~730 parses across the build. Reducing it by 1,000 saves ~730,000 parses. **This is where the 18-minute Vercel builds are coming from.**

### Tier 1 — confirmed shell-graph leaks (data from analyzer, not theory)

Every entry below was produced by counting how many of the 948 routes have a given file's full path inside their `analyze.data` source list.

| # | Item | Routes pulling it | What's actually true | Recommendation | Status |
|---|---|---|---|---|---|
| 12 | **`lucide-react` icon-per-file explosion** | **731 routes**, **1,712 distinct icon modules** in the chunk graph | `lucide-react@0.577.0` ships every icon as a separate ESM file. `optimizePackageImports` shrinks the *bundle*, but Turbopack still has to parse every individual icon module Turbopack reaches via static analysis. With 731 page entries × ~hundreds of icon modules each, this is the single largest module-parse pile in the build. By module-count, **lucide alone is 20% of the per-route static graph**. | Same trick that worked for `@lobehub/icons`: enumerate the icons actually used in shell-reachable files (Sidebar, Header, MobileDock, NavActiveSync, settings shells, official card-and-grid set, top-level `components/ui/*`), inline those SVGs into a single `components/icons/lucide-inline.tsx`, then sweep the rest in a few passes. Long tail can stay on `lucide-react` behind dynamic boundaries; only the shell needs to be cut. **This is the highest-leverage single change available right now.** | ▢ |
| 13 | **`monaco-editor` ESM tree pulled into 715 routes** | **715 routes**, **1,043 monaco modules** | All four direct `from "monaco-editor"` imports in the codebase are `import type` (verified). Yet the analyzer shows 1,043 monaco ESM files materialized into the static graph of 715 page entries. Two possible mechanisms: (a) `@monaco-editor/react`'s type definitions reference `monaco-editor` types, and Turbopack walks the package's full export map to resolve them; (b) one of the `code-editor`/`code` files imports a *value* from monaco we missed (the `CodeEditorContextMenu`, `MonacoEditor.tsx`, `monaco-config.ts` paths look suspect). Even if it's (a), Turbopack is parsing 1,043 monaco files in 715 chunks. That's ~745k parses just for monaco. | (1) Re-grep ALL monaco-related imports — not just `from "monaco-editor"` but also `@monaco-editor/react` named imports and `import * as monaco`. (2) Confirm the editor entry points are actually behind a `dynamic({ ssr: false })` boundary at every consumer. (3) If (a) is the cause: replace `import type { editor } from "monaco-editor"` with hand-rolled minimal type aliases (`type MonacoEditor = { /* the 4-5 methods we use */ }`) so Turbopack stops resolving monaco's package altogether at the type layer. | ▢ |
| 14 | **`@cartesia/cartesia-js` in 716 routes** | **716 routes**, **296 modules** | The Cartesia TTS SDK is a feature-specific dep, but somehow 75% of routes are pulling it. Most direct importers are properly scoped (`features/audio/voice/*`, `hooks/tts/*`, chat `MessageOptionsMenu`). The likely shell-leak path is `constants/voice-options.ts` (statically imports `availableVoices` from `lib/cartesia/voices.ts`) — anything that imports `voice-options` or `lib/cartesia/voices` from an always-loaded path drags Cartesia in. The 2,243-LOC `lib/cartesia/voices.ts` is itself one of the top 30 largest source files in the repo. | Trace the import chain from any shell-reachable file (Providers/DeferredSingletons → audio islands → cartesia). Likely fix is the same "thin shell + Impl" pattern at the entry: whatever currently `import`s from `@cartesia/cartesia-js` synchronously needs a tiny passthrough that `await import()`s the SDK lazily. Move `lib/cartesia/voices.ts` to a `lib/cartesia/voices.lazy.ts` async loader for consumers that don't need the full list at module-init. | ▢ |
| 15 | **`features/prompt-builtins/*` in 721 routes** | **721 routes** for `constants.ts`, `services/admin-service.ts`, `utils/error-handler.ts` | This is a feature-specific module set, not shell. The smoking gun is **`features/context-menu/UnifiedContextMenu.tsx`** (1,759 LOC — fourth-largest file in the codebase) which directly imports `useUnifiedContextMenu` from `prompt-builtins/hooks/useUnifiedContextMenu`. `app/(ssr)/ssr/notes/_components/NoteContextMenuContent.tsx` and `features/prompts/components/layouts/ConvertToBuiltinModal.tsx` also pull from `prompt-builtins/constants` directly. Per CLAUDE.md, `features/context-menu` is on the migration list (being replaced by agent-shortcuts). 721 routes is essentially "every authenticated/SSR/public route" — something widely-loaded is reaching `UnifiedContextMenu` or one of its peers. | (1) Find the actual shell consumer — likely a context-menu provider or a chat input component that's mounted everywhere. (2) Either dynamic-import it like the other heavy leaves (RecoveryWindow, GlobalTaskShortcut), or — given the active prompts→agents migration — accelerate the replacement. (3) Specifically, `prompt-builtins/services/admin-service.ts` should never be in a client chunk graph; it's an admin service. If it's reachable from non-admin pages, that's a leak through a barrel or a shared util. | ▢ |
| 16 | **`refractor` (highlight.js for Prism) in 715 routes, `highlight.js` in 715, `react-syntax-highlighter` in 715** | 715 / 715 / 715 routes; 306 + 192 + 65 modules respectively (~563 modules total) | `react-syntax-highlighter` defaults to importing every language grammar (190+) when used through the heavy entry point. Both `react-syntax-highlighter` and `refractor` are in 715 routes — the `Prism`/`Light` split is the canonical fix here. With a deep import (`react-syntax-highlighter/dist/cjs/light` + only registering languages we actually highlight), the language grammars drop out of the static graph. | Audit the 8 importers of `react-syntax-highlighter`. Switch to `light` build + manual language registration (the typical 6-8 languages: ts, js, tsx, jsx, python, json, bash, sql). Expected drop: ~200+ modules out of 715 chunks each. | ▢ |
| 17 | **`lodash` in 716 routes, 244 modules** | **716 routes** | Even with tree-shaking, every distinct lodash function is its own file. Some files in the repo use `import _ from 'lodash'` (the heavy default), others use `lodash/x` deep imports, and `lodash-es` is also installed as a transitive. Of these, only `lodash-es` is on Next.js's auto-`optimizePackageImports` list. | Sweep: replace `from 'lodash'` with `from 'lodash-es'` (or `es-toolkit` for the simpler functions). Audit deep-import callsites for redundant utilities (`_.get`, `_.set`, `_.isEqual` are usually replaceable with native or 5-line helpers). | ▢ |
| 18 | **`@tabler/icons-react` in 730 routes** | **730 routes**, 100 modules | Smaller magnitude than lucide (100 vs 1,712 modules), but every authenticated/public route inherits it. Already on the auto-`optimizePackageImports` list (so bundle size is fine), but the same parse-cost story applies as lucide — Turbopack still walks every referenced icon module. | Lower priority than lucide (17x fewer modules), but the same playbook applies if the long tail of admin/dashboard tabs is using only a handful of `@tabler` icons that could be inlined. | ▢ |

### What the data also showed (and what's NOT a problem)

- **`components/overlays/OverlayController.tsx` (2,569 LOC, 4th largest file)** — does NOT appear in the top-50 by route count. The "thin shell + Impl" refactor is doing its job; the 860-LOC `windowRegistry` is correctly behind `next/dynamic`. ✅
- **Entity system (`lib/redux/entity/selectors.ts` 2,270 LOC, `sagaHandlers.ts` 1,697 LOC, `initialSchemas.ts` 116k LOC, `initialTableSchemas.ts` 109k LOC)** — none of them appear in the top-50 by route count. The slim-store / entity-store split is working: `EntityProviders` is only mounted by `(legacy)/layout.tsx`, so the 200k+ LOC schema chain stays out of every other route's static graph. The recent `import type` pass on `reduxTypes.ts` and `emptyGlobalCache.ts` held. ✅
- **`features/code-editor`, `features/code/*`** — only appears in `/a/code` and a handful of admin routes, not the shell. ✅
- **`@xyflow/react` (workflow canvas, 2.7 MB)** — feature-scoped to workflows. ✅
- **`three`, `mermaid`, `@babel/standalone`, `posthog-js`, `onnxruntime-web`** — all confirmed lazy/CDN-loaded. Not in shell graph. ✅
- **The 730-route project-file shell baseline** is itself reasonable: `app/layout.tsx`, `app/not-found.tsx`, the official card/grid set, ui primitives (button/input/label/toast), `lib/utils`, `lib/sync/*`, `constants/navigation-links.tsx`, theme + user preferences slices. None of these are individually fixable — they're the legitimate shell. ✅

### Recommended next moves, in order of guaranteed impact

1. **Lucide-react inline (#12)** — the single biggest win. By module count this is 20% of every route's static graph. Same surgical-extraction approach that retired `@lobehub/icons` (22m → 9m). Realistic estimate: another 2-4 minute drop on Vercel after the next deploy.
2. **prompt-builtins shell leak (#15)** — find which globally-mounted component reaches `UnifiedContextMenu` (or `NoteContextMenuContent`, or `ConvertToBuiltinModal`), then dynamic-boundary it. Given the active prompts→agents migration, this might be solved naturally — but right now it's contaminating 721 routes' graphs.
3. **Cartesia shell leak (#14)** — likely a `constants/voice-options.ts` chain or an audio toast widget. Cut at the import root. ~300 modules × 716 routes recovered.
4. **react-syntax-highlighter `light` build (#16)** — 200+ language grammars dropped from 715 routes' graphs. Pure config change at the import sites.
5. **Monaco type-walk (#13)** — if the lucide and prompt-builtins fixes don't move the needle enough, dig into why Turbopack is materializing 1,043 monaco modules per route despite all imports being `import type`. May need hand-rolled type aliases to fully sever.

Tier 2 items #6, #7, #8 from the original list are now confirmed:
- #6 `framer-motion` (104 modules in 721 routes) — auto-optimized but still showing up; lower-priority than lucide.
- #7 `react-syntax-highlighter` — confirmed problem, addressed in item #16 above.
- #8 `@xyflow/react` — confirmed properly isolated to workflow routes only. Skip.

---

## Change log

- **2026-04-28 (am)** — Doc created. Lobehub removal verified done by Arman (inlined SVGs). Corrections recorded for `onnxruntime-web` and the "unused" folder claim.
- **2026-04-28 (pm)** — Monaco type-imports task closed as a false alarm (already optimal; runtime is CDN-loaded). `pdfjs-dist` removed from `package.json` dependencies; `pnpm install` reconciled to a single `5.4.296` via `react-pdf`. Verified working on PDF routes.
- **2026-04-28 (late pm)** — Global shell audit, first pass (#9). Refactored `app/DeferredSingletons.tsx`, `app/Providers.tsx`, and `app/(public)/PublicProviders.tsx`: dropped the `@/lib/redux/brokerSlice` barrel import, lazy-loaded `brokerActions` and `fetchFullContext` inside idle callbacks, and wrapped 12 leaf widgets in `next/dynamic({ ssr: false })` from inside the wrappers themselves. **This was wrong** — `Providers.tsx` is a Server Component (where `ssr: false` is invalid) and the dynamic gating belongs at the leaf level, not the wrapper level. Reverted all three wrappers to clean static-import shells.
- **2026-04-28 (evening)** — Global shell audit, corrected pass (#9). Adopted the "thin shell + Impl" pattern at the leaf level. For each of 7 heavy leaf widgets — `RecoveryWindow`, `GlobalTaskShortcut`, `AudioRecoveryToast`, `AuthSessionWatcher`, `AnnouncementProvider`, `UnifiedOverlayController`, `LegacyPromptOverlaysController` — split into two files: `<Name>.tsx` (tiny `"use client"` shell, optionally gated on a cheap selector or local event listener, renders `<Name>Impl />` via `next/dynamic({ ssr: false, loading: () => null })`) and `<Name>Impl.tsx` (the existing heavy body, default-exported). For `CloudFilesPickerHost`, split three ways: `cloudFilesPickerOpeners.ts` (pure-TS module-level opener registry + imperative API), `CloudFilesPickerHostImpl.tsx` (the heavy dialog body), and `CloudFilesPickerHost.tsx` (thin shell that re-exports the imperative API and renders the Impl dynamically) — preserves every existing import path. `RecoveryNudge`, `CreateTaskFromSourceDialog`, `PersistentDOMConnector`, and `AdminFeatureProvider` (which already followed the pattern) were left alone. ReadLints: clean across all 20 touched files.
- **2026-04-28 (analyzer-run)** — Ran `pnpm build:analyze:save` locally (7.8min). Wrote a parallel parser at `scripts/analyze-routes.py` that walks all 948 route `analyze.data` files, reconstructs full source paths via `parent_source_index` chain, and aggregates module reach. Saved full report to `/tmp/route-analysis.txt`. Key finding: build-time bottleneck is not a few fat routes — it's a fat *baseline* graph (median 8,674 project-file modules per route, P90 8,792, max 9,702 — only 12% variance) that 730+ routes inherit. The actionable shell-graph leaks ranked by route reach: `lucide-react` (1,712 modules × 731 routes = 1.25M parses), `monaco-editor` (1,043 × 715), `@cartesia/cartesia-js` (296 × 716), `prompt-builtins/*` (3 files × 721 routes via `features/context-menu/UnifiedContextMenu.tsx`), `refractor`+`highlight.js`+`react-syntax-highlighter` (563 × 715), `lodash` (244 × 716). Confirmed that the prior shell+entity-isolation passes held: `OverlayController`, entity system (`selectors.ts`, `sagaHandlers.ts`, `initialSchemas.ts`, `initialTableSchemas.ts`), `code-editor`, `@xyflow/react`, `three`, `mermaid`, `@babel/standalone`, `onnxruntime-web` are NOT in the top-50 cross-route module list. New Tier 1 items #12-#18 added with concrete recommendations.
- **2026-04-28 (night)** — Layout-reachable type-only audit (#10). Searched for the biggest TS files in the repo — top hits were `utils/schema/initialSchemas.ts` (115,992 LOC), `initialTableSchemas.ts` (108k), `database.types.ts` (24k), `python-generated/api-types.ts` (15k), `socket-schema.ts` (4.3k). Traced who static-imports each, then cross-referenced against the layout/provider chain. Two systemic issues stood out: (a) `types/reduxTypes.ts` — imported by 21 files, including every layout, every redux store factory, and most slice creators — was importing `Database`, `UserData`, and `GlobalCacheState` as plain named imports despite using all three only as types; this forced Turbopack to walk `database.types.ts` (24k LOC) and `globalCacheSlice.ts` (which transitively reaches `entityTypes` → `initialSchemas.ts` 115k LOC) for every chunk that touched `reduxTypes`. (b) `utils/schema/schema-processing/emptyGlobalCache.ts` — invoked at module-scope by `(authenticated)`, `(a)`, and `(ssr)` layouts — used non-type imports of `EntityKeys`, `AnyEntityDatabaseTable`, `AutomationEntities`, `UnifiedSchemaCache`, `SchemaEntity`, `FullEntityRelationships`, `RelationshipDetails`. All were used purely as types. Converted every type-only usage to `import type` across `reduxTypes.ts`, `emptyGlobalCache.ts`, the three slim layouts (`InitialReduxState`/`UserData`), `features/ai-models/types.ts` (`Database`), `lib/redux/store.ts` (`UserPreferences`/`UserPreferencesState`), and the 9 redux slice/saga/dynamic/feature consumers. Added a build-time-contract doc comment at the top of both `reduxTypes.ts` and `emptyGlobalCache.ts` so future agents understand why every named import in those files must be type-only. ReadLints clean across all 16 touched files. Verified `next.config.js` `optimizePackageImports` is already optimal — the auto-optimized default list (per Next.js docs) covers `@tabler/icons-react`, `react-icons/*`, `framer-motion`, `recharts`, `date-fns`, `lodash-es`, `lucide-react`, etc.; explicit listing is a no-op merge. The slim path's static graph is now fully decoupled from the entity schema system and from the 24k-LOC Supabase generated types — the (legacy) layout still legitimately preloads them by design.
