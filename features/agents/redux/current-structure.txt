Code Context  [mode: signatures  —  signatures — code skeleton (functions/classes/methods, no bodies)]
Scanned: features/agents/redux
Files: 50  |  Directories: 16

Here is the code for the module I want you to review.
A directory tree is shown first, followed by the code.

features/agents/redux/
├── agent-consumers/
│   ├── selectors.ts
│   ├── slice.ts
├── agent-definition/
│   ├── converters.ts
│   ├── rpc-parsers.ts
│   ├── selectors.ts
│   ├── slice.ts
│   ├── thunks.ts
│   ├── types.ts
├── agent-shortcuts/
│   ├── converters.ts
│   ├── selectors.ts
│   ├── slice.ts
│   ├── thunks.ts
│   ├── types.ts
├── execution-system/
│   ├── active-requests/
│   │   ├── active-requests.selectors.ts
│   │   ├── active-requests.slice.ts
│   │   ├── index.ts
│   ├── execution-instances/
│   │   ├── execution-instances.selectors.ts
│   │   ├── execution-instances.slice.ts
│   │   ├── index.ts
│   ├── instance-client-tools/
│   │   ├── index.ts
│   │   ├── instance-client-tools.selectors.ts
│   │   ├── instance-client-tools.slice.ts
│   ├── instance-context/
│   │   ├── index.ts
│   │   ├── instance-context.selectors.ts
│   │   ├── instance-context.slice.ts
│   ├── instance-conversation-history/
│   │   ├── index.ts
│   │   ├── instance-conversation-history.selectors.ts
│   │   ├── instance-conversation-history.slice.ts
│   ├── instance-model-overrides/
│   │   ├── index.ts
│   │   ├── instance-model-overrides.selectors.ts
│   │   ├── instance-model-overrides.slice.ts
│   ├── instance-resources/
│   │   ├── index.ts
│   │   ├── instance-resources.selectors.ts
│   │   ├── instance-resources.slice.ts
│   ├── instance-ui-state/
│   │   ├── index.ts
│   │   ├── instance-ui-state.selectors.ts
│   │   ├── instance-ui-state.slice.ts
│   ├── instance-user-input/
│   │   ├── index.ts
│   │   ├── instance-user-input.selectors.ts
│   │   ├── instance-user-input.slice.ts
│   ├── instance-variable-values/
│   │   ├── index.ts
│   │   ├── instance-variable-values.selectors.ts
│   │   ├── instance-variable-values.slice.ts
│   ├── selectors/
│   │   ├── aggregate.selectors.ts
│   │   ├── instance-model-overrides.selectors.ts
│   ├── thunks/
│   │   ├── create-instance.thunk.ts
│   │   ├── execute-instance.thunk.ts
│   ├── utils/
│   │   ├── ids.ts
│   │   ├── index.ts
│   │   ├── source-slice-helpers.ts



---
Filepath: features/agents/redux/execution-system/instance-model-overrides/instance-model-overrides.slice.ts  [typescript]

  export interface InstanceModelOverridesState
  initInstanceOverrides( state, action: PayloadAction<{ instanceId: string; baseSettings?: Partial<LLMParams> }>, )
  setOverrides( state, action: PayloadAction<{ instanceId: string; changes: Partial<LLMParams>; }>, )
  if (entry)
  markRemoved( state, action: PayloadAction<{ instanceId: string; key: string }>, )
  resetOverride( state, action: PayloadAction<{ instanceId: string; key: string }>, )
  resetAllOverrides(state, action: PayloadAction<string>)
  removeInstanceOverrides(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-model-overrides/instance-model-overrides.selectors.ts  [typescript]

  for (const key of overrideState.removals)
  export const selectInstanceOverrideState = (instanceId: string) =>
  export const selectCurrentSettings = (instanceId: string) =>
  export const selectSettingsOverridesForApi = (instanceId: string) =>
  export const selectHasOverrides = (instanceId: string) =>
  export const selectOverriddenKeys = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-model-overrides/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-variable-values/instance-variable-values.selectors.ts  [typescript]

  for (const def of definitions)
  if (def.name in userValues)
  if (def.name in scopeValues)
  return ( def.defaultValue === undefined || def.defaultValue === null || def.defaultValue === "" )
  export const selectInstanceVariableDefinitions = (instanceId: string) =>
  export const selectUserVariableValues = (instanceId: string) =>
  export const selectScopeVariableValues = (instanceId: string) =>
  export const selectResolvedVariables = (instanceId: string) =>
  export const selectMissingRequiredVariables = (instanceId: string) =>
  export const selectVariableProvenance = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-variable-values/instance-variable-values.slice.ts  [typescript]

  export interface InstanceVariableValuesEntry
  export interface InstanceVariableValuesState
  initInstanceVariables( state, action: PayloadAction<{ instanceId: string; /** Snapshot of variable definitions from the agent. Required for isolation. */ definitions?: VariableDefinition[]; scopeValues?: Record<string, unknown>; }>, )
  setUserVariableValue( state, action: PayloadAction<{ instanceId: string; name: string; value: unknown; }>, )
  if (entry)
  setUserVariableValues( state, action: PayloadAction<{ instanceId: string; values: Record<string, unknown>; }>, )
  clearUserVariableValue( state, action: PayloadAction<{ instanceId: string; name: string }>, )
  setScopeVariableValues( state, action: PayloadAction<{ instanceId: string; values: Record<string, unknown>; }>, )
  resetUserVariableValues(state, action: PayloadAction<string>)
  removeInstanceVariables(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-variable-values/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-user-input/instance-user-input.selectors.ts  [typescript]

  export const selectUserInputText = (instanceId: string) =>
  export const selectUserInputContentBlocks = (instanceId: string) =>
  export const selectHasUserInput = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-user-input/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-user-input/instance-user-input.slice.ts  [typescript]

  export interface InstanceUserInputSliceState
  initInstanceUserInput( state, action: PayloadAction<{ instanceId: string; text?: string }>, )
  setUserInputText( state, action: PayloadAction<{ instanceId: string; text: string }>, )
  if (entry)
  setUserInputContentBlocks( state, action: PayloadAction<{ instanceId: string; blocks: Array<Record<string, unknown>>; }>, )
  clearUserInput(state, action: PayloadAction<string>)
  removeInstanceUserInput(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-conversation-history/instance-conversation-history.selectors.ts  [typescript]

  export interface AggregateStats
  export interface AggregateClientMetrics
  for (let i = turns.length - 1; i >= 0; i--)
  if (turns[i].role === "assistant" && turns[i].completionStats)
  for (const turn of turns)
  if (t)
  if (turns[i].role === "assistant" && turns[i].clientMetrics)
  if (m.ttftMs !== null)
  if (m.internalLatencyMs !== null)
  if (m.totalClientDurationMs !== null)
  export const selectConversationTurns = (instanceId: string) =>
  export const selectConversationMode = (instanceId: string) =>
  export const selectStoredConversationId = (instanceId: string) =>
  export const selectTurnCount = (instanceId: string) =>
  export const selectHasConversationHistory = (instanceId: string) =>
  export const selectLoadedFromHistory = (instanceId: string) =>
  export const selectLatestCompletionStats = (instanceId: string) =>
  export const selectAggregateStats = (instanceId: string) =>
  export const selectLatestClientMetrics = (instanceId: string) =>
  export const selectAggregateClientMetrics = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-conversation-history/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-conversation-history/instance-conversation-history.slice.ts  [typescript]

  export type ConversationMode = "agent" | "conversation" | "chat"
  export interface TokenUsage
  export interface ConversationTurn
  export interface InstanceConversationHistoryEntry
  export interface InstanceConversationHistoryState
  function newTurnId(): string
  initInstanceHistory( state, action: PayloadAction<{ instanceId: string; mode?: ConversationMode; }>, )
  if (!state.byInstanceId[instanceId])
  addUserTurn( state, action: PayloadAction<{ instanceId: string; content: string; contentBlocks?: Array<Record<string, unknown>>; conversationId?: string | null; }>, )
  commitAssistantTurn( state, action: PayloadAction<{ instanceId: string; requestId: string; content: string; conversationId: string | null; tokenUsage?: TokenUsage; finishReason?: string; completionStats?: CompletionStats; }>, )
  if (conversationId && !entry.conversationId)
  if (entry.mode === "agent")
  attachClientMetrics( state, action: PayloadAction<{ instanceId: string; requestId: string; clientMetrics: ClientMetrics; }>, )
  if (turn)
  loadConversationHistory( state, action: PayloadAction<{ instanceId: string; turns: ConversationTurn[]; conversationId: string; mode?: ConversationMode; }>, )
  clearHistory(state, action: PayloadAction<string>)
  if (entry)
  removeInstanceHistory(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/execution-instances/execution-instances.selectors.ts  [typescript]

  export const selectInstance = (instanceId: string) =>
  export const selectAllInstanceIds = (state: RootState): string[] =>
  export const selectInstancesByAgent = (agentId: string) =>
  export const selectInstanceStatus = (instanceId: string) =>
  export const selectRunningInstances = (state: RootState): ExecutionInstance[] =>


---
Filepath: features/agents/redux/execution-system/execution-instances/execution-instances.slice.ts  [typescript]

  export interface ExecutionInstancesState
  createInstance( state, action: PayloadAction<{ instanceId?: string; agentId: string; agentType: AgentType; origin: InstanceOrigin; shortcutId?: string; status?: InstanceStatus; }>, )
  setInstanceStatus( state, action: PayloadAction<{ instanceId: string; status: InstanceStatus; }>, )
  if (instance)
  destroyInstance(state, action: PayloadAction<string>)
  destroyInstancesForAgent(state, action: PayloadAction<string>)
  for (const id of toRemove)


---
Filepath: features/agents/redux/execution-system/execution-instances/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/thunks/create-instance.thunk.ts  [typescript]

  interface CreateManualInstanceArgs
  interface CreateShortcutInstanceArgs
  interface CreateTestInstanceArgs
  interface CreateManualInstanceNoAgentArgs
  interface ReInstanceAndExecuteArgs
  interface ReInstanceAndExecuteResult
  function readAgentSnapshot( state: RootState, agentId: string, ):
  for (const mapping of mappings)
  if (mapping.mapTo === "variable")
  if (contextEntries.length > 0)
  if (!instance)
  if (userInputText)
  onNewInstance(newInstanceId)


---
Filepath: features/agents/redux/execution-system/thunks/execute-instance.thunk.ts  [typescript]

  interface ExecuteInstanceArgs
  interface ExecuteInstanceResult
  interface SubmitToolResultsArgs
  export function assembleRequest( state: RootState, instanceId: string, ): AssembledAgentStartRequest | null
  if (resourcePayloads.length > 0)
  if (!instance)
  if (!payload)
  if (!baseUrl)
  if (accessToken)
  if (userInputText || userContentBlocks)
  if (existingConversationId)
  if (!response.ok)
  if (conversationId)
  if ( data.event === "conversation_id" && typeof data.conversation_id === "string" && !conversationId )
  if (toolData.event === "tool_delegated")
  if (totalUsage)
  if (!request?.conversationId)
  for (const result of results)


---
Filepath: features/agents/redux/execution-system/active-requests/active-requests.selectors.ts  [typescript]

  for (const req of requests)
  if (req.parentConversationId)
  if (!children[req.parentConversationId])
  export const selectRequest = (requestId: string) =>
  export const selectRequestsForInstance = (instanceId: string) =>
  export const selectPrimaryRequest = (instanceId: string) =>
  export const selectRequestStatus = (requestId: string) =>
  export const selectAccumulatedText = (requestId: string) =>
  export const selectRequestConversationId = (requestId: string) =>
  export const selectUnresolvedToolCalls = (requestId: string) =>
  export const selectConversationTree = (instanceId: string) =>
  export const selectHasActiveRequests = (state: RootState): boolean =>


---
Filepath: features/agents/redux/execution-system/active-requests/active-requests.slice.ts  [typescript]

  export interface ActiveRequestsState
  createRequest( state, action: PayloadAction<{ requestId?: string; instanceId: string; parentConversationId?: string | null; }>, )
  if (!state.byInstanceId[instanceId])
  setRequestStatus( state, action: PayloadAction<{ requestId: string; status: RequestStatus; errorMessage?: string; }>, )
  if (request)
  if ( status === "complete" || status === "error" || status === "timeout" )
  setConversationId( state, action: PayloadAction<{ requestId: string; conversationId: string; }>, )
  appendChunk( state, action: PayloadAction<{ requestId: string; content: string }>, )
  if (!request.firstChunkAt)
  appendDataPayload( state, action: PayloadAction<{ requestId: string; data: Record<string, unknown>; }>, )
  addPendingToolCall( state, action: PayloadAction<{ requestId: string; toolCall: Omit< PendingToolCall, "receivedAt" | "deadlineAt" | "resolved" >; }>, )
  resolveToolCall( state, action: PayloadAction<{ requestId: string; callId: string }>, )
  if (call)
  if (allResolved && request.status === "awaiting-tools")
  addWarning( state, action: PayloadAction<{ requestId: string; warning: StreamWarning; }>, )
  finalizeClientMetrics( state, action: PayloadAction<{ requestId: string; metrics: ClientMetrics }>, )
  removeRequest(state, action: PayloadAction<string>)
  if (instanceRequests)
  if (state.byInstanceId[request.instanceId].length === 0)
  for (const reqId of requestIds)


---
Filepath: features/agents/redux/execution-system/active-requests/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-client-tools/instance-client-tools.slice.ts  [typescript]

  export interface InstanceClientToolsState
  initInstanceClientTools( state, action: PayloadAction<{ instanceId: string; tools?: string[]; }>, )
  addClientTool( state, action: PayloadAction<{ instanceId: string; toolName: string }>, )
  removeClientTool( state, action: PayloadAction<{ instanceId: string; toolName: string }>, )
  if (tools)
  setClientTools( state, action: PayloadAction<{ instanceId: string; tools: string[]; }>, )
  removeInstanceClientTools(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-client-tools/instance-client-tools.selectors.ts  [typescript]

  export const selectInstanceClientTools = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-client-tools/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/utils/source-slice-helpers.ts  [typescript]

  export function createSourceInitialState<T>(): SourceSliceState<T>
  export function upsertEntity<T extends object>( state: Draft<SourceSliceState<T>>, id: string, data: T, ): void
  export function removeEntity<T>( state: Draft<SourceSliceState<T>>, id: string, ): void
  export function markDirty<T>( state: Draft<SourceSliceState<T>>, id: string, ): void
  export function clearDirty<T>( state: Draft<SourceSliceState<T>>, id: string, ): void
  export function createAsyncHandlers<T>()


---
Filepath: features/agents/redux/execution-system/utils/ids.ts  [typescript]

  export const generateInstanceId = (): string =>
  export const generateResourceId = (): string =>
  export const generateRequestId = (): string =>
  export const generateScopeId = (): string =>


---
Filepath: features/agents/redux/execution-system/utils/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-context/instance-context.selectors.ts  [typescript]

  for (const entry of entries)
  export const selectInstanceContextEntries = (instanceId: string) =>
  export const selectInstanceContextEntry = (instanceId: string, key: string) =>
  export const selectSlotMatchedContext = (instanceId: string) =>
  export const selectAdHocContext = (instanceId: string) =>
  export const selectContextPayload = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-context/instance-context.slice.ts  [typescript]

  export interface InstanceContextState
  function inferType(value: unknown): ContextObjectType
  if (typeof value === "string")
  initInstanceContext(state, action: PayloadAction<{ instanceId: string }>)
  setContextEntry( state, action: PayloadAction<{ instanceId: string; key: string; value: unknown; slotMatched?: boolean; type?: ContextObjectType; label?: string; }>, )
  if (context)
  setContextEntries( state, action: PayloadAction<{ instanceId: string; entries: Array<{ key: string; value: unknown; slotMatched?: boolean; type?: ContextObjectType; label?: string; }>; }>, )
  for (const entry of entries)
  removeContextEntry( state, action: PayloadAction<{ instanceId: string; key: string }>, )
  clearInstanceContext(state, action: PayloadAction<string>)
  removeInstanceContext(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-context/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-resources/instance-resources.selectors.ts  [typescript]

  switch (r.blockType)
  if (typeof content === "string")
  export const selectInstanceResources = (instanceId: string) =>
  export const selectResource = (instanceId: string, resourceId: string) =>
  export const selectReadyResources = (instanceId: string) =>
  export const selectPendingResources = (instanceId: string) =>
  export const selectAllResourcesResolved = (instanceId: string) =>
  export const selectResourcePayloads = (instanceId: string) =>


---
Filepath: features/agents/redux/execution-system/instance-resources/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-resources/instance-resources.slice.ts  [typescript]

  export interface InstanceResourcesState
  initInstanceResources( state, action: PayloadAction<{ instanceId: string }>, )
  addResource( state, action: PayloadAction<{ instanceId: string; blockType: ResourceBlockType; source: unknown; options?: Partial<ResourceOptions>; resourceId?: string; }>, )
  if (resources)
  setResourceStatus( state, action: PayloadAction<{ instanceId: string; resourceId: string; status: ResourceStatus; errorMessage?: string; }>, )
  if (resource)
  setResourcePreview( state, action: PayloadAction<{ instanceId: string; resourceId: string; preview: unknown; }>, )
  setResourceEditedContent( state, action: PayloadAction<{ instanceId: string; resourceId: string; content: unknown; }>, )
  setResourcePayload( state, action: PayloadAction<{ instanceId: string; resourceId: string; payload: Record<string, unknown>; }>, )
  updateResourceOptions( state, action: PayloadAction<{ instanceId: string; resourceId: string; options: Partial<ResourceOptions>; }>, )
  removeResource( state, action: PayloadAction<{ instanceId: string; resourceId: string; }>, )
  reorderResources( state, action: PayloadAction<{ instanceId: string; orderedIds: string[]; }>, )
  if (resources[id])
  clearAllResources(state, action: PayloadAction<string>)
  if (entry)
  removeInstanceResources(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/instance-ui-state/instance-ui-state.selectors.ts  [typescript]

  export const selectInstanceUIState = (instanceId: string) =>
  export const selectDisplayMode = (instanceId: string) =>
  export const selectIsModalFull = (instanceId: string) =>
  export const selectIsModalCompact = (instanceId: string) =>
  export const selectIsChatBubble = (instanceId: string) =>
  export const selectIsInline = (instanceId: string) =>
  export const selectIsPanel = (instanceId: string) =>
  export const selectIsToast = (instanceId: string) =>
  export const selectIsAnyModal = (instanceId: string) =>
  export const selectAllowChat = (instanceId: string) =>
  export const selectShowVariablePanel = (instanceId: string) =>
  export const selectSubmitOnEnter = (instanceId: string) =>
  export const selectIsCreator = (instanceId: string) =>
  export const selectShowCreatorDebug = (instanceId: string) =>
  export const selectExpandedVariableId = (instanceId: string) =>
  export const selectAutoClearConversation = (instanceId: string) =>
  export const selectIsExpanded = (instanceId: string) =>
  export const selectModeState = (instanceId: string) =>
  export const selectInstanceIdsByMode = (mode: ResultDisplayMode) =>


---
Filepath: features/agents/redux/execution-system/instance-ui-state/index.ts  [typescript]



---
Filepath: features/agents/redux/execution-system/instance-ui-state/instance-ui-state.slice.ts  [typescript]

  export interface InstanceUIStateSlice
  initInstanceUIState( state, action: PayloadAction<{ instanceId: string; displayMode?: ResultDisplayMode; allowChat?: boolean; showVariablePanel?: boolean; isCreator?: boolean; submitOnEnter?: boolean; autoClearConversation?: boolean; }>, )
  setDisplayMode( state, action: PayloadAction<{ instanceId: string; mode: ResultDisplayMode; }>, )
  if (entry)
  toggleExpanded(state, action: PayloadAction<string>)
  toggleVariablePanel(state, action: PayloadAction<string>)
  setAllowChat( state, action: PayloadAction<{ instanceId: string; allow: boolean }>, )
  updateModeState( state, action: PayloadAction<{ instanceId: string; changes: Record<string, unknown>; }>, )
  setExpandedVariableId( state, action: PayloadAction<{ instanceId: string; variableId: string | null }>, )
  toggleCreatorDebug(state, action: PayloadAction<string>)
  setSubmitOnEnter( state, action: PayloadAction<{ instanceId: string; value: boolean }>, )
  setAutoClearConversation( state, action: PayloadAction<{ instanceId: string; value: boolean }>, )
  removeInstanceUIState(state, action: PayloadAction<string>)


---
Filepath: features/agents/redux/execution-system/selectors/aggregate.selectors.ts  [typescript]

  return ( state.activeRequests.byRequestId[ids[ids.length - 1]]?.status === "connecting" )
  return ( state.activeRequests.byRequestId[ids[ids.length - 1]]?.startedAt ?? undefined )
  return ( state.activeRequests.byRequestId[ids[ids.length - 1]]?.errorMessage ?? undefined )
  if (!instance)
  if (resources)
  if (pending.length > 0)
  if (errored.length > 0)
  for (const def of definitions)
  if (!hasValue)
  assembleRequest(state, instanceId)
  export const selectIsExecuting = (instanceId: string) =>
  export const selectIsStreaming = (instanceId: string) =>
  export const selectIsAwaitingTools = (instanceId: string) =>
  export const selectLatestAccumulatedText = (instanceId: string) =>
  export const selectLatestConversationId = (instanceId: string) =>
  export const selectConversationMode = (instanceId: string) =>
  export const selectLatestRequestId = (instanceId: string) =>
  export const selectLatestRequestStatus = (instanceId: string) =>
  export const selectIsConnecting = (instanceId: string) =>
  export const selectIsWaitingForFirstToken = (instanceId: string) =>
  export const selectLatestRequestStartedAt = (instanceId: string) =>
  export const selectLatestError = (instanceId: string) =>
  export const selectPendingToolCallsForInstance = (instanceId: string) =>
  export const selectHasAnyContent = (instanceId: string) =>
  export const selectIsInstanceReady = (instanceId: string) =>
  export const selectAssembledRequest = (instanceId: string) =>
  export const selectInstanceSummary = (instanceId: string) =>
  export const makeSelectInstanceDisplaySnapshot = () =>
  export const selectAvailableShortcuts = (context: ShortcutContext) =>


---
Filepath: features/agents/redux/execution-system/selectors/instance-model-overrides.selectors.ts  [typescript]

  for (const key of overrideState.removals)
  export const selectInstanceOverrideState = (instanceId: string) =>
  export const selectCurrentSettings = (instanceId: string, agentId: string) =>
  export const selectSettingsOverridesForApi = (instanceId: string, agentId: string) =>
  export const selectHasOverrides = (instanceId: string) =>
  export const selectOverriddenKeys = (instanceId: string) =>


---
Filepath: features/agents/redux/agent-shortcuts/converters.ts  [typescript]

  type ShortcutRow = Database["public"]["Tables"]["agent_shortcuts"]["Row"]
  type ShortcutInsert = Omit< Database["public"]["Tables"]["agent_shortcuts"]["Insert"], "id" | "created_at" | "updated_at" >
  type ShortcutUpdate = Omit< Database["public"]["Tables"]["agent_shortcuts"]["Update"], "id" | "created_at" | "updated_at" >
  export function dbRowToAgentShortcut(row: ShortcutRow): AgentShortcut
  export function agentShortcutToInsert(shortcut: AgentShortcut): ShortcutInsert
  export function agentShortcutToUpdate( partial: Partial<AgentShortcut>, ): ShortcutUpdate


---
Filepath: features/agents/redux/agent-shortcuts/thunks.ts  [typescript]

  type ThunkApi =
  interface FetchShortcutsForContextArgs
  if (error)
  for (const placement of results)
  for (const categoryGroup of placement.menu_data)
  for (const item of categoryGroup.shortcuts)
  if (item.resolved_id && item.agent)
  dispatch( mergePartialAgent({ id: item.resolved_id, name, variableDefinitions: variable_definitions, contextSlots: context_slots ?? [], isVersion, parentAgentId: isVersion ? (item.agent_id ?? null) : null, }), )
  for (const row of rows)
  if (row.resolved_id && row.agent_variable_definitions !== undefined)
  dispatch( mergePartialAgent({ id: row.resolved_id, variableDefinitions: row.agent_variable_definitions, contextSlots: row.agent_context_slots ?? [], isVersion, parentAgentId: isVersion ? (row.agent_id ?? null) : null, }), )


---
Filepath: features/agents/redux/agent-shortcuts/types.ts  [typescript]

  export interface AgentShortcut
  export interface AgentShortcutInitialRow
  export interface AgentShortcutContextRow extends AgentShortcutInitialRow
  export interface AgentShortcutMenuResult
  export interface AgentShortcutCategory
  export interface AgentShortcutMenuItem
  export interface UserShortcutItem
  export interface CreateShortcutForAgentParams
  export type ShortcutFieldSnapshot =
  export type ShortcutLoadedFields = Set<keyof AgentShortcut>
  export interface AgentShortcutRecord extends AgentShortcut
  export interface AgentShortcutSliceState


---
Filepath: features/agents/redux/agent-shortcuts/selectors.ts  [typescript]

  const selectAgentShortcutSlice = (state: RootState) =>


---
Filepath: features/agents/redux/agent-shortcuts/slice.ts  [typescript]

  function makeEmptyRecord(id: string): AgentShortcutRecord
  function mergeAndTrack( record: AgentShortcutRecord, partial: Partial<AgentShortcut>, ): void
  function applyFieldEdit<K extends keyof AgentShortcut>( record: AgentShortcutRecord, field: K, value: AgentShortcut[K], ): void
  function markRecordClean(record: AgentShortcutRecord): void
  if (partial[key] !== undefined)
  upsertShortcut(state, action: PayloadAction<AgentShortcut>)
  if (existing)
  mergeAndTrack(existing, data)
  markRecordClean(existing)
  mergeAndTrack(record, data)
  markRecordClean(record)
  upsertShortcuts(state, action: PayloadAction<AgentShortcut[]>)
  mergePartialShortcut( state, action: PayloadAction<Partial<AgentShortcut> & { id: string }>, )
  setShortcutField( state, action: PayloadAction<{ id: string; field: keyof AgentShortcut; value: AgentShortcut[keyof AgentShortcut]; }>, )
  applyFieldEdit(record, field, value as AgentShortcut[typeof field])
  setShortcutEnabledContexts( state, action: PayloadAction<{ id: string; enabledContexts: AgentShortcut["enabledContexts"]; }>, )
  applyFieldEdit(record, "enabledContexts", action.payload.enabledContexts)
  setShortcutScopeMappings( state, action: PayloadAction<{ id: string; scopeMappings: AgentShortcut["scopeMappings"]; }>, )
  applyFieldEdit(record, "scopeMappings", action.payload.scopeMappings)
  resetShortcutField( state, action: PayloadAction<{ id: string; field: keyof AgentShortcut }>, )
  if (original !== undefined)
  resetAllShortcutFields(state, action: PayloadAction<{ id: string }>)
  markShortcutSaved(state, action: PayloadAction<{ id: string }>)
  rollbackShortcutOptimisticUpdate( state, action: PayloadAction<{ id: string; snapshot: ShortcutFieldSnapshot }>, )
  if (value !== undefined)
  setShortcutLoading( state, action: PayloadAction<{ id: string; loading: boolean }>, )
  setShortcutError( state, action: PayloadAction<{ id: string; error: string | null }>, )
  setActiveShortcutId(state, action: PayloadAction<string | null>)
  setInitialLoaded(state, action: PayloadAction<boolean>)
  setContextLoaded( state, action: PayloadAction<{ key: string; loaded: boolean }>, )
  setShortcutsStatus( state, action: PayloadAction<AgentShortcutSliceState["status"]>, )
  setShortcutsError(state, action: PayloadAction<string | null>)
  removeShortcut(state, action: PayloadAction<string>)
  if (state.activeShortcutId === action.payload)


---
Filepath: features/agents/redux/agent-consumers/selectors.ts  [typescript]

  export function computeAgentSearchScore( agent: AgentDefinitionRecord, query: string, ): number
  export function agentMatchesSearch( agent: AgentDefinitionRecord, query: string, ): boolean
  export function applyAgentSortComparator( a: AgentDefinitionRecord, b: AgentDefinitionRecord, sortBy: AgentSortOption, ): number
  switch (sortBy)
  for (const a of agents)
  if (includedCats.length > 0)
  if (isUncategorized)
  if (includedTags.length > 0)
  if (isUntagged)
  if (searchTerm)
  if (favoritesFirst && favFilter === "all")
  const makeSelectAgentConsumerState = (consumerId: string) =>
  export const makeSelectFilteredAgents = (consumerId: string) =>
  export const makeSelectFilteredBuiltinAgents = (consumerId: string) =>
  export const makeSelectAgentCards = (consumerId: string, isMobile: boolean) =>
  export const makeSelectAgentListItems = ( consumerId: string, isMobile: boolean, ) =>
  export const makeSelectFilteredAgentsCount = (consumerId: string) =>
  export const makeSelectFilteredBuiltinAgentsCount = (consumerId: string) =>
  export const makeSelectAgentConsumerHasActiveFilters = (consumerId: string) =>
  export const makeSelectAgentSearchTerm = (consumerId: string) =>
  export const makeSelectAgentSortBy = (consumerId: string) =>
  export const makeSelectAgentTab = (consumerId: string) =>


---
Filepath: features/agents/redux/agent-consumers/slice.ts  [typescript]

  export type AgentSortOption = | "updated-desc" | "created-desc" | "name-asc" | "name-desc" | "category-asc"
  export type AgentTab = "mine" | "shared" | "all" | "system"
  export type AgentFavFilter = "all" | "yes" | "no"
  export type AgentArchFilter = "active" | "archived" | "both"
  export type AgentAccessFilter = "any" | "owned" | "shared" | "editable"
  export interface AgentConsumerState
  export interface AgentConsumersState
  if (!state.consumers[id])
  if (!state.consumers[consumerId])
  if (state.consumers[action.payload])
  export const selectAgentConsumer = ( state: RootState, consumerId: string, ): AgentConsumerState =>
  export const selectAllAgentConsumers = (state: RootState) =>


---
Filepath: features/agents/redux/agent-definition/converters.ts  [typescript]

  type AgentRow = Database["public"]["Tables"]["agents"]["Row"]
  type AgentInsert = Omit< Database["public"]["Tables"]["agents"]["Insert"], "id" | "created_at" | "updated_at" | "source_agent_id" | "source_snapshot_at" >
  type AgentUpdate = Omit< Database["public"]["Tables"]["agents"]["Update"], "id" | "created_at" | "updated_at" | "source_agent_id" | "source_snapshot_at" >
  export function dbRowToAgentDefinition(row: AgentRow): AgentDefinition
  export function agentDefinitionToInsert(agent: AgentDefinition): AgentInsert
  export function agentDefinitionToUpdate( partial: Partial<AgentDefinition>, ): AgentUpdate


---
Filepath: features/agents/redux/agent-definition/thunks.ts  [typescript]

  type ThunkApi =
  export interface AgentVersionHistoryItem
  export interface AgentVersionSnapshot
  export interface SharedAgentItem
  export interface SharedAgentForChat
  export interface AgentAccessLevel
  export interface PurgeVersionsResult
  if (error)
  for (const row of rows)
  if (result.success)


---
Filepath: features/agents/redux/agent-definition/types.ts  [typescript]

  export type AgentType = "user" | "builtin"
  export type VariableComponentType = | "textarea" // Default — multi-line text | "toggle" // Boolean with custom on/off labels | "radio" // Single-select from options | "checkbox" // Multi-select from options | "select" // Dropdown single-select | "number"
  export interface VariableCustomComponent
  export type AccessLevel = | "owner" | "admin" | "editor" | "viewer" | "system" | "public" | "none"
  export interface VariableDefinition
  export interface ModelTier
  export interface ModelTiers
  export interface AgentDefinition
  export interface AgentListRow
  export interface DuplicateAgentParams
  export type DuplicateAgentResult = string
  export interface PromoteVersionParams
  export interface PromoteVersionResult
  export interface AgentExecutionMinimal
  export interface AgentExecutionFull
  export interface AgentDriftItem
  export interface AgentReference
  export interface AcceptVersionResult
  export interface UpdateFromSourceResult
  export type AgentFetchStatus = | "list" | "execution" | "customExecution" | "full" | "versionSnapshot"
  export type FieldSnapshot =
  export type LoadedFields = Set<keyof AgentDefinition>
  export interface AgentDefinitionRecord extends AgentDefinition
  export interface AgentDefinitionSliceState
  export function shouldUpgradeFetchStatus( current: AgentFetchStatus | null, incoming: AgentFetchStatus, ): boolean


---
Filepath: features/agents/redux/agent-definition/selectors.ts  [typescript]

  if (!record)
  const selectAgentDefinitionSlice = (state: RootState) =>


---
Filepath: features/agents/redux/agent-definition/rpc-parsers.ts  [typescript]

  type Primitive = "string" | "number" | "boolean"
  function assertField( row: Record<string, unknown>, key: string, type: Primitive, rpc: string, ): void
  function assertOptionalField( row: Record<string, unknown>, key: string, type: Primitive, rpc: string, ): void
  function assertStringArray( row: Record<string, unknown>, key: string, rpc: string, ): void
  function toRow(raw: unknown, rpc: string): Record<string, unknown>
  export function parseExecutionMinimal(raw: unknown): AgentExecutionMinimal
  export function parseExecutionFull(raw: unknown): AgentExecutionFull
  export function parseVersionHistoryItem(raw: unknown): AgentVersionHistoryItem
  export function parseVersionSnapshot(raw: unknown): AgentVersionSnapshot
  export function parseAccessLevel(raw: unknown): AgentAccessLevel
  export function parseDriftItem(raw: unknown): AgentDriftItem
  export function parseAgentReference(raw: unknown): AgentReference
  export function parsePromoteVersionResult(raw: unknown): PromoteVersionResult
  export function parseAcceptVersionResult(raw: unknown): AcceptVersionResult
  export function parseUpdateFromSourceResult( raw: unknown, ): UpdateFromSourceResult
  export function parsePurgeVersionsResult( raw: unknown, ): import("./thunks").PurgeVersionsResult
  if (typeof row[key] !== type)
  if (key in row && row[key] !== null && typeof row[key] !== type)
  if (raw === null || raw === undefined)
  assertField(row, "id", "string", rpc)
  assertOptionalField(row, "model_id", "string", rpc)
  assertField(row, "version_id", "string", rpc)
  assertField(row, "version_number", "number", rpc)
  assertField(row, "name", "string", rpc)
  assertField(row, "changed_at", "string", rpc)
  assertField(row, "is_active", "boolean", rpc)
  assertField(row, "agent_id", "string", rpc)
  assertField(row, "agent_name", "string", rpc)
  assertField(row, "access_level", "string", rpc)
  assertField(row, "is_owner", "boolean", rpc)
  assertField(row, "reference_id", "string", rpc)
  assertField(row, "version_pinned_to", "number", rpc)
  assertField(row, "current_version", "number", rpc)
  assertField(row, "versions_behind", "number", rpc)
  assertField(row, "reference_name", "string", rpc)
  assertField(row, "reference_type", "string", rpc)
  assertField(row, "use_latest", "boolean", rpc)
  assertField(row, "is_behind", "boolean", rpc)
  assertField(row, "success", "boolean", rpc)


---
Filepath: features/agents/redux/agent-definition/slice.ts  [typescript]

  function makeEmptyRecord(id: string): AgentDefinitionRecord
  function normalizeMessages( messages: AgentDefinition["messages"], ): AgentDefinition["messages"]
  function mergeAndTrack( record: AgentDefinitionRecord, partial: Partial<AgentDefinition>, ): void
  function applyFieldEdit<K extends keyof AgentDefinition>( record: AgentDefinitionRecord, field: K, value: AgentDefinition[K], ): void
  function markRecordClean(record: AgentDefinitionRecord): void
  function applyFetchStatus( record: AgentDefinitionRecord, status: AgentFetchStatus, ): void
  if ( raw.type === "text" && raw.text === undefined && raw.content !== undefined )
  if (normalized[key] !== undefined)
  upsertAgent(state, action: PayloadAction<AgentDefinition>)
  if (existing)
  mergeAndTrack(existing, data)
  markRecordClean(existing)
  applyFetchStatus(existing, status)
  mergeAndTrack(record, data)
  markRecordClean(record)
  applyFetchStatus(record, status)
  mergePartialAgent( state, action: PayloadAction<Partial<AgentDefinition> & { id: string }>, )
  setAgentField( state, action: PayloadAction<{ id: string; field: keyof AgentDefinition; value: AgentDefinition[keyof AgentDefinition]; }>, )
  applyFieldEdit(record, field, value as AgentDefinition[typeof field])
  setAgentMessages( state, action: PayloadAction<{ id: string; messages: AgentDefinition["messages"]; }>, )
  applyFieldEdit(record, "messages", action.payload.messages)
  setAgentSettings( state, action: PayloadAction<{ id: string; settings: AgentDefinition["settings"]; }>, )
  applyFieldEdit(record, "settings", action.payload.settings)
  setAgentVariableDefinitions( state, action: PayloadAction<{ id: string; variableDefinitions: AgentDefinition["variableDefinitions"]; }>, )
  applyFieldEdit( record, "variableDefinitions", action.payload.variableDefinitions, )
  setAgentContextSlots( state, action: PayloadAction<{ id: string; contextSlots: AgentDefinition["contextSlots"]; }>, )
  applyFieldEdit(record, "contextSlots", action.payload.contextSlots)
  setAgentTools( state, action: PayloadAction<{ id: string; tools: AgentDefinition["tools"] }>, )
  applyFieldEdit(record, "tools", action.payload.tools)
  setAgentCustomTools( state, action: PayloadAction<{ id: string; customTools: AgentDefinition["customTools"]; }>, )
  applyFieldEdit(record, "customTools", action.payload.customTools)
  setAgentModelTiers( state, action: PayloadAction<{ id: string; modelTiers: AgentDefinition["modelTiers"]; }>, )
  applyFieldEdit(record, "modelTiers", action.payload.modelTiers)
  setAgentOutputSchema( state, action: PayloadAction<{ id: string; outputSchema: AgentDefinition["outputSchema"]; }>, )
  applyFieldEdit(record, "outputSchema", action.payload.outputSchema)
  resetAgentField( state, action: PayloadAction<{ id: string; field: keyof AgentDefinition }>, )
  if (original !== undefined)
  resetAllAgentFields(state, action: PayloadAction<{ id: string }>)
  markAgentSaved(state, action: PayloadAction<{ id: string }>)
  rollbackAgentOptimisticUpdate( state, action: PayloadAction<{ id: string; snapshot: FieldSnapshot }>, )
  if (value !== undefined)
  setAgentFetchStatus( state, action: PayloadAction<{ id: string; status: AgentFetchStatus }>, )
  applyFetchStatus(record, action.payload.status)
  setAgentLoading( state, action: PayloadAction<{ id: string; loading: boolean }>, )
  setAgentError( state, action: PayloadAction<{ id: string; error: string | null }>, )
  setActiveAgentId(state, action: PayloadAction<string | null>)
  setAgentsStatus( state, action: PayloadAction<AgentDefinitionSliceState["status"]>, )
  setAgentsError(state, action: PayloadAction<string | null>)
  removeAgent(state, action: PayloadAction<string>)
  if (state.activeAgentId === action.payload)
  removeVersionsByParentId(state, action: PayloadAction<string>)
  if (record.isVersion && record.parentAgentId === action.payload)

Please review carefully and let me know if anything needs to be improved.