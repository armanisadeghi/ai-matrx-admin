// Workflow management hooks
export { useWorkflowSync } from './useWorkflowSync';
export { useWorkflowEdges } from './useWorkflowEdges';
export { useProcessConnection } from './useProcessConnection';

// Note: Both hooks use the same workflowId prop pattern
