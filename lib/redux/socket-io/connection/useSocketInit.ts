"use client";

// Socket.io has been disabled — the server no longer supports it.
// This hook is a no-op to prevent connection attempts.

export function useSocketInit() {
  // Intentionally empty — socket.io is no longer supported by the backend.
}
